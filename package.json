{
  "name": "token-counter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:vite": "tsc && vite build",
    "build:content": "esbuild src/content/index.ts --bundle --outfile=dist/content.js --external:./assets/*",
    "build": "npm run build:vite && npm run build:content",
    "preview": "vite preview"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^24.10.0",
    "rollup-plugin-copy": "^3.5.0",
    "typescript": "^5.5.3",
    "vite": "^5.3.3"
  }
}